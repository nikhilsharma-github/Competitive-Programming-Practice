#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    long long test;
    cin >> test;
    while (test--)
    {
        long long n,count=0;
        cin >> n;

        //  for(long long i=1;i<=n;i++){

        //      if((n%i)<=float(n/2))
        //      count++;

        //  }


         cout<<n<<endl;
    }
    return 0;
}