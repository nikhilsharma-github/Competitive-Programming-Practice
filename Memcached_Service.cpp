#include <bits/stdc++.h>
using namespace std;
int main()
{
ios_base::sync_with_stdio(false);
cin.tie(NULL);
int test;
cin >> test;
while (test--)
{
    int n;
    cin>>n;
    vector<string> s;
    for(int i=0;i<n;i++)
    {
        cin>>s[i];
    }
    int pc=0;
    for(int i=0;i<n;i++)
    {
        if(pc==0&&s[i])
    }
}
return 0;
}