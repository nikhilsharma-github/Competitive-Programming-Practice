#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
     int ld,sld,pd;
    long long m,k;
    cin >> m;
    k=m;
    if (m >= 0)
        cout << m << endl;
    else
    {
    //  ld=m%10;
    //  m=m/10;
    //  sld=m%10;
     while(m)
     {
         pd=m%10;
         
     }
    }

    long long temp1,temp2,count=1;
    temp1=m;
    temp2=m;
     temp1=temp1-(ld*1);
     temp2=temp2-(sld*10);
    // while(temp1)
    // {
    //    pd=temp1%10;
    //    if(pd==ld)
    //    {
        
    //    }
    //    temp1=temp1/10;
    //    count++;
    // }
    count=1;
    return 0;
}