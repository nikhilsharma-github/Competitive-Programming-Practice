#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    long long test;
    cin >> test;
    while (test--)
    {
        string s, c;
        cin >> s >> c;
        sort(s.begin(), s.end());
        sort(c.begin(), c.end());
        int count = 0,countk=0,res=0,m=0, s1 = s.size(), s2 = c.size();
         m=max(s1,s2);
        while(count<m){
               if(s[count])

        }
        // cout<<s<<endl;
        // cout<<c<<endl;
        while ()
    }
    return 0;
}