#include <bits/stdc++.h>
using namespace std;
int main()
{
ios_base::sync_with_stdio(false);
cin.tie(NULL);
long long test,t,d,fc;
cin >> test;
while (test--)
{
    list <int> vf,vc;
long long f,c;
cin >> f>>c;

  for(int i=0;i<f;i++)
  {
      cin>>t;
      vf.push_back(t);
  }
  for(int i=0;i<d;i++)
  {
      cin>>t;
      vc.push_back(t);
  }

      int sf,sc;
      sf=vf.size();
      sc=vc.size();
      int mxx=max(sf,sc);

       int i=1;

       while(i<=mxx){

             

       }
}
return 0;
}